{{- if .Values.deviceStats.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{- include "uem.deviceStatsFullname" . }}
  labels:
    {{- include "uem.deviceStatsLabels" . | nindent 4 }}
spec:
  {{- if not .Values.deviceStats.autoscaling.enabled }}
  replicas: {{ .Values.deviceStats.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "uem.deviceStatsSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.deviceStats.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "uem.deviceStatsSelectorLabels" . | nindent 8 }}
    spec:
      {{- include "uem.imagePullSecrets" (dict "root" $ "imagePullSecrets" .Values.deviceStats.imagePullSecrets) | nindent 6 }}
      {{/*      serviceAccountName: {{ include "helm-charts.serviceAccountName" . }}*/}}
      securityContext:
        {{- toYaml .Values.deviceStats.podSecurityContext | nindent 8 }}
      {{- include "wait-for-emqx" . | nindent 6 }}
      containers:
        - name: {{- include "uem.deviceStatsFullname" . }}
          securityContext:
            {{- toYaml .Values.deviceStats.securityContext | nindent 12 }}
          image: {{ include "uem.image" (dict "root" $ "image" .Values.deviceStats.image) }}
          imagePullPolicy: {{ .Values.deviceStats.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 9085
              protocol: TCP
            - name: grpc
              containerPort: 9086
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            failureThreshold: 3
            initialDelaySeconds: 60
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            failureThreshold: 3
            initialDelaySeconds: 10
            timeoutSeconds: 10
          volumeMounts:
            - mountPath: /data/conf/config.yaml
              subPath: config.yaml
              name: config
            {{- include "middleware-config" . | nindent 10 }}
          resources:
            {{- toYaml .Values.deviceStats.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{- include "uem.deviceStatsFullname" . }}
        - name: middleware-config
          configMap:
            name: middleware
      {{- with .Values.deviceStats.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.deviceStats.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.deviceStats.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end -}}