{{- if and .Values.remoteProxy.enabled .Values.remoteProxy.metrics .Values.monitoring.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "uem.remoteProxyFullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- toYaml .Values.monitoring.labels | nindent 4 }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "uem.remoteProxyFullname" . }}
  endpoints:
  - port: http
  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}
{{- end -}}