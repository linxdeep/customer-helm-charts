{{- if .Values.processor.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "uem.processorFullname" . }}
  labels:
    {{- include "uem.processorLabels" . | nindent 4 }}
spec:
  {{- if not .Values.processor.autoscaling.enabled }}
  replicas: {{ .Values.processor.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "uem.processorSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.processor.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "uem.processorSelectorLabels" . | nindent 8 }}
    spec:
      {{- include "wait-for-uemctl" . | nindent 6 }}
      {{- include "uem.imagePullSecrets" (dict "root" $ "imagePullSecrets" .Values.processor.imagePullSecrets) | nindent 6 }}
      {{/*      serviceAccountName: {{ include "helm-charts.serviceAccountName" . }}*/}}
      securityContext:
        {{- toYaml .Values.processor.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ include "uem.processorFullname" . }}
          securityContext:
            {{- toYaml .Values.processor.securityContext | nindent 12 }}
          image: {{ include "uem.image" (dict "root" $ "image" .Values.processor.image) }}
          imagePullPolicy: {{ .Values.processor.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 9089
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            failureThreshold: 3
            initialDelaySeconds: 60
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            failureThreshold: 3
            initialDelaySeconds: 10
            timeoutSeconds: 10
          volumeMounts:
            - mountPath: /data/conf/config.yaml
              subPath: config.yaml
              name: config
            {{- include "middleware-config" . | nindent 10 }}
          resources:
            {{- toYaml .Values.processor.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "uem.processorFullname" . }}
        - name: middleware-config
          configMap:
            name: middleware
      {{- with .Values.processor.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.processor.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.processor.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end -}}