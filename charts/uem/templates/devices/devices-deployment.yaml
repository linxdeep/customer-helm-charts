{{- if .Values.devices.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "uem.devicesFullname" . }}
  labels:
    {{- include "uem.devicesLabels" . | nindent 4 }}
spec:
  {{- if not .Values.devices.autoscaling.enabled }}
  replicas: {{ .Values.devices.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "uem.devicesSelectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.devices.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "uem.devicesSelectorLabels" . | nindent 8 }}
    spec:
      {{- include "wait-for-uemctl" . | nindent 6 }}
      {{- include "uem.imagePullSecrets" (dict "root" $ "imagePullSecrets" .Values.devices.imagePullSecrets) | nindent 6 }}
      {{/*      serviceAccountName: {{ include "helm-charts.serviceAccountName" . }}*/}}
      securityContext:
        {{- toYaml .Values.devices.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ include "uem.devicesFullname" . }}
          securityContext:
            {{- toYaml .Values.devices.securityContext | nindent 12 }}
          image: {{ include "uem.image" (dict "root" $ "image" .Values.devices.image) }}
          imagePullPolicy: {{ .Values.devices.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 9090
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            failureThreshold: 3
            initialDelaySeconds: 60
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            periodSeconds: 30
            failureThreshold: 3
            initialDelaySeconds: 10
            timeoutSeconds: 10
          volumeMounts:
            - mountPath: /data/conf/config.yaml
              name: config
              subPath: config.yaml
            {{- include "middleware-config" . | nindent 10 }}
          resources:
            {{- toYaml .Values.devices.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "uem.devicesFullname" . }}
        - name: middleware-config
          configMap:
            name: middleware
      {{- with .Values.devices.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.devices.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.devices.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end -}}